platform :ios, '8.1'
use_frameworks!

target 'FUMC' do
  pod 'NSDate+TimeAgo', :inhibit_warnings => true
  pod 'SwiftMoment', :inhibit_warnings => true
  pod 'Locksmith', '2.0.8'

  target 'FUMCTests' do
    inherit! :search_paths
  end
end
